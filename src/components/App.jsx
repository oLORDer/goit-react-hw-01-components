import Profile from './Profile';

export const App = props => {
  return (
    console.log(props),
    (
      <Profile
      // username={user.username}
      // tag={user.tag}
      // location={user.location}
      // avatar={user.avatar}
      // stats={user.stats}
      />
    )
  );
};
